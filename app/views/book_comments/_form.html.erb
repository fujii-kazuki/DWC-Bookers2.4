<%= form_with(model: [book, book_comment], local: false) do |f| %>
  <%= f.text_area :comment, id: 'comment-text-area', class: 'form-control mb-2' %>
  <%= f.submit '送信', class: 'btn btn-sm btn-primary' %>
<% end %>